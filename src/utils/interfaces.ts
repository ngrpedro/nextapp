export interface User {
  _id: number;
  name: string;
}

export interface IPartner {
  _id: string;
  name: string;
  reception: string;
  city: string;
  isInstitute: boolean;
  colege: string;
  about: string;
  tags: string;
}
